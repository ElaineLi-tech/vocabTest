# 英语词汇量测试网站 PRD

## 1. 项目概述

### 1.1 项目背景
基于开源词库 [KyleBing/english-vocabulary](https://github.com/KyleBing/english-vocabulary) 构建一个中文用户友好的英语词汇量测试网站。词库覆盖小学 → 初中 → 高中 → 四级 → 六级 → 考研 → 专四/专八 → 雅思 → 托福 → SAT → GRE → GMAT → 商务英语 共 23 本教材、总计 116,953 条词条（正序版），数据丰富且分级明确，适合做词汇量评估。

### 1.2 产品目标
- 帮助用户在 3–5 分钟内快速估算自身英语词汇量
- 提供分级别的能力诊断（每个词汇等级的掌握比例）
- 提供后续学习建议，并支持导出测试报告
- 纯静态网站部署，零后端依赖，数据隐私完全保留在本地

### 1.3 目标用户
- 中小学生、大学生、考研/留学备考人群
- 职场人士评估英语水平
- 英语学习者阶段性自测进步

---

## 2. 词源数据说明

### 2.1 数据来源
路径：`/Users/yilingli/VocabTest/english-vocabulary/full_line_jsonl/sentence/正序/`

### 2.2 推荐使用格式（sentence 正序）
每个词书对应一个 `.jsonl` 文件，每行为一个 JSON 对象，字段如下：

| 字段 | 类型 | 说明 |
|------|------|------|
| `word` | string | 单词原形 |
| `us` | string | 美式音标 |
| `uk` | string | 英式音标 |
| `translations` | array | 释义列表，每项含 `translation`（中文释义）和 `type`（词性 n/v/adj…） |
| `phrases` | array | 短语列表，每项含 `phrase`（短语）和 `translation`（中文释义） |
| `sentences` | array | 例句列表，每项含 `sentence`（英文例句）和 `translation`（中文翻译） |

示例（talk）：
```json
{
  "word": "talk",
  "us": "tɔk",
  "uk": "tɔːk",
  "translations": [
    { "translation": "说话；谈话", "type": "v" },
    { "translation": "交谈", "type": "n" }
  ],
  "phrases": [
    { "phrase": "talk about", "translation": "谈论某事" }
  ],
  "sentences": [
    { "sentence": "I could hear Sarah and Andy talking in the next room.",
      "translation": "我听到萨拉和安迪在隔壁讲话。" }
  ]
}
```

### 2.3 词汇等级映射（按难度从低到高）
将 23 本书归并为 **10 个难度档位**，用于分层抽样和词汇量估算：

| 档位 | 难度 | 包含词书 | 总词数（正序 sentence） |
|------|------|----------|----------------------:|
| L1 | 入门 | 人教小学三/四/五/六年级 | 849 |
| L2 | 初中基础 | 人教初中七/八/九年级 + 外研社初中 + 初中 | 约 6,705 |
| L3 | 高中基础 | 人教高中 + 北师高中 + 高中 | 约 13,178 |
| L4 | 四级 CET-4 | 四级 | 7,508 |
| L5 | 六级 CET-6 | 六级 | 5,651 |
| L6 | 考研 / 专四 | 考研 + 专四 | 14,222 |
| L7 | 雅思 / GMAT / 商务英语 | 雅思 + GMAT + 商务英语 | 约 18,881 |
| L8 | 托福 / 专八 | 托福 + 专八 | 约 26,358 |
| L9 | SAT | SAT | 8,887 |
| L10 | GRE | GRE | 13,714 |

> **注**：具体去重后词数需在数据预处理阶段合并去重得出精确值。上述数字为原始目录的"正序 sentence"版本累计，做词汇量估计时使用去重后的档位词汇表作为基数。

---

## 3. 功能需求

### 3.1 首页（欢迎页）
**页面要素**：
- 标题：「你的英语词汇量有多少？」+ 副标题 3–5 分钟完成估算
- 开始测试按钮（主 CTA，突出）
- 两种测试模式切换：
  - **快速模式**（默认，约 40 题）
  - **精准模式**（约 80 题，结果更准）
- 测试说明卡片：
  1. 看到单词后，判断自己是否「认识」
  2. 不确定请选「不认识」，避免虚高
  3. 认识的单词会出现 4 选 1 释义题，需要答对才算掌握
- 历史记录入口（从 localStorage 读取，可选清空）

### 3.2 测试页面（核心流程）
采用 **自适应分层抽样算法**：
1. 初始从中等难度 L4/L5 开始
2. 每档根据正确率决定是否继续深入更难档位，或回退补测更简单档位
3. 全程实时显示进度条（已答题数 / 总题数）

**每题卡片要素**：
- 单词（大字体）+ 英美音标（🔊 图标，点击播放发音，优先使用 Web Speech API）
- 题目类型：
  - **判断题（认识 / 不认识）**：先出现，用户选"认识"才进入选择题
  - **四选一中英释义题**：正确释义来自词条 `translations`，干扰项从同档位其他词的释义中随机抽取
- 答题反馈：
  - 答对：✅ 绿色提示，显示例句
  - 答错/不认识：❌ 红色提示，显示完整释义 + 短语 + 例句
- 键盘快捷键：`1/2/3/4` 选择、`Space` 确认、`←`/`→` 翻页（仅回顾场景）

**算法规则（简化版自适应）**：
- 每题赋一个档位分（L1=1，… L10=10）
- 用户"认识且答对"记为「掌握」，"不认识或答错"记为「未掌握」
- 每个档位抽样 5–10 个词，当该档位正确率 ≥ 60% 时，下一题向更高难度偏移；< 30% 则向更低难度偏移
- 达到总题数上限或档位连续 3 次正确率 < 20% 时提前结束

### 3.3 结果页面
**五大模块**：

#### 模块 1：总体词汇量估算
- 巨型数字：例如「**8,420**」
- 副标题：「超过全国约 85% 的大学生」（用内置百分位对照，纯展示参考）
- 估算公式：
  ```
  每档已掌握比例 = 该档掌握数 / 该档抽样数
  每档贡献词量 = 该档已掌握比例 × 该档总词量
  总词汇量 = Σ 每档贡献词量
  ```
- 置信区间：展示「± 5%」等误差范围，快速模式区间更大

#### 模块 2：词汇量对照参照表
将用户的估算词汇量与官方课标 / 考试线 / 全球百分位对齐展示；**根据用户测出的数字自动高亮对应的一行**（背景色填充 + 左侧箭头指示）。

| 词汇量区间 | 中国学段 / 考试对应（官方） | 掌握水平描述 | 超过约 %（全球非母语自测者 \*） |
|------------|------------------------------|--------------|---------------------------------|
| 500 ~ 1,000 | 小学高年级（课标小学段 505） | 基础生存词汇 | 约超过 2 ~ 8% |
| 1,000 ~ 2,000 | 初中（义务教育课标 2022 要求 1,600） | 基本词汇，日常听说读写入门 | 约超过 8 ~ 15% |
| 2,000 ~ 3,000 | 高中必修→选择性必修（课标 2,000~3,200）；旧课标高考线 3,500 | 能应对中等难度阅读，高考基础盘 | 约超过 15 ~ 25% |
| 3,000 ~ 4,500 | 高考高分段 / 四级（大纲 4,500，含中学基础约 1,500） | 大学基础英语，可读一般英文材料 | 约超过 25 ~ 40% |
| 4,500 ~ 6,000 | 六级（5,500~6,000）/ 考研（5,500） | 较流利阅读，学术入门 | 约超过 40 ~ 55% |
| 6,000 ~ 8,000 | 专四（7,000~8,000）/ 雅思 7 分线 | 能应付留学场景、复杂观点表达 | 约超过 55 ~ 75% |
| 8,000 ~ 10,000 | 托福（9,000~10,000）/ 雅思 8 分线 | 学术英语较强，接近流畅 | 约超过 75 ~ 90% |
| 10,000 ~ 13,000 | 专八（约 13,000） | 高学术水平，文学 / 语言学词汇 | 约超过 90 ~ 95% |
| 13,000 ~ 20,000 | GRE（16,000~20,000） | 高强度学术 / 考试词汇 | 约超过 95 ~ 99% |
| 20,000+ | 母语成人水平（成年母语者 20,000~35,000） | 接近 / 达到母语者阅读词汇 | 约超过 99% |

> 说明：
> 1. 百分位数据仅作参考展示，可后续替换为真实统计基准；
> 2. 移动端表格可折叠为卡片形式；
> 3. 高亮行需同时在页内"副标题"的描述文字上保持一致。

#### 模块 3：分级能力雷达图 / 柱状图
- 横轴：L1–L10 10 个档位（标注中文等级名）
- 纵轴：各档掌握百分比（0–100%）
- 鼠标悬停显示：该档抽样 X 题 / 答对 Y 题 / 估算掌握 Z 词
- 柱状图下方用不同颜色标识掌握程度（<30% 红色 / 30–70% 黄色 / >70% 绿色）

#### 模块 4：学习建议 + 详细清单
- 薄弱档位 Top 3 推荐：
  - 例如：「你的 L6（考研/专四）档掌握率仅 35%，建议优先复习考研核心词汇」
  - 每档列出 10 个未掌握的关键词供立即查看
- 已掌握档位：鼓励语 + 推荐挑战更高级别词书
- **导出按钮**：
  - 导出 PNG 分享卡片（带词汇量数字、能力分布图、日期水印）
  - 导出未掌握单词列表（Markdown / TXT，可直接打印默写）

#### 模块 5：领取学习资料（微信二维码占位）
- **位置**：结果页底部（移动端独占一屏，PC 端在左侧或右侧与学习建议并排）
- **卡片标题**：「🎁 截图保存你的词汇量，加微信领取专属学习包」
- **内容结构**：
  1. 微信二维码占位框（`200×200 px`，设计稿里放入灰色占位图 + 文案「微信二维码」，后续替换为真实图片 `src/assets/wechat-qrcode.png`）
  2. 二维码下方一行小字：微信号：`your-wechat-id`（可配置）
  3. 领取说明列表：
     - 第 1 步：点击「保存结果截图」按钮，保存你的词汇量卡片 PNG
     - 第 2 步：长按/扫描上方二维码，添加微信并发送截图
     - 第 3 步：免费领取 ① 分级核心词汇 Excel ② 艾宾浩斯复习计划表 ③ 常用词根词缀表（共 3 份电子资料）
- **按钮**：在二维码下方放一个小按钮，方便第 1 步：「一键保存结果截图」（复用导出 PNG 逻辑）
- **可配置**：二维码图片路径、微信号、学习包内容列表均放在 `src/config/wx.ts`，不改动业务代码即可替换。

### 3.4 历史记录
- 本地保存最近 20 次测试结果（localStorage）
- 折线图展示词汇量随时间变化
- 支持删除单条记录 / 清空全部
- 每次记录点击可回看当时的详细结果

### 3.5 默写练习（可选的扩展页）
- 用户可以从某次结果的"未掌握词汇"清单一键进入默写练习
- 默写格式遵循用户偏好：**左边中文释义，右边下划线（无表格）**
- 支持打印和保存 PDF

---

## 4. 非功能需求

### 4.1 性能
- 首次加载 < 2 秒（打包后 < 500KB gzipped；词库按需分包加载，不一次性加载 86MB）
- 每页切换和答题响应 < 100ms
- 支持离线使用（Service Worker + 缓存已加载的词书）

### 4.2 兼容性
- 现代浏览器：Chrome / Safari / Edge / Firefox 最近 2 个大版本
- 移动端：iOS Safari 14+ / Android Chrome 90+，完全响应式
- 不兼容 IE 系列

### 4.3 数据与隐私
- 所有测试数据、历史记录仅存于用户浏览器 localStorage
- 不发送任何用户行为数据到服务器
- 可一键"清除我的所有数据"

### 4.4 UI / 设计语言
- 整体风格：简洁、干净、教育类应用质感
- 配色：浅色主色（蓝绿色系）+ 中性灰；模式切换 / 深色模式可选
- 排版：单词使用大号无衬线字体（如 Inter），释义使用等宽或衬线中文字体以区分
- 遵循用户偏好：**文字应位于水平线之上；关键文字必须完全在其 bounding box 内；整体设计语言保持一致**

---

## 5. 技术方案

### 5.1 技术栈
- **框架**：React 18 + Vite（单页应用，构建产物为纯静态文件）
- **样式**：Tailwind CSS 3 + CSS Variables（深色模式）
- **图表**：Recharts（柱状图 + 雷达图 + 折线图）
- **导出图片**：html2canvas（PNG 分享卡片）
- **词库加载**：动态 import() 分档加载 `.jsonl`（预处理成紧凑 JSON，每档一个文件）
- **发音**：Web Speech API（`SpeechSynthesis`），无需后端 TTS
- **部署**：GitHub Pages / Vercel / Netlify 任一静态托管

### 5.2 数据预处理（构建前脚本）
1. 从 `english-vocabulary/full_line_jsonl/sentence/正序/` 读取所有词书
2. 按 §2.3 的 10 个档位合并去重（以 `word.toLowerCase()` 为 key，保留首次出现的完整释义）
3. 生成 `src/data/levels/L{1..10}.json`，每个文件结构：
   ```json
   {
     "level": 4,
     "name": "CET-4 四级",
     "total": 7508,
     "words": [
       { "w": "ability", "us": "...", "uk": "...",
         "t": [{"v":"能力，能耐；才能","p":"n"}],
         "s": [{"e":"...","c":"..."}] }
     ]
   }
   ```
   （使用短字段名压缩体积，总目标 ≤ 10MB 全部档位合计）
4. 额外生成 `src/data/levels/index.json` 汇总每档的总词数和简介

### 5.3 目录结构
```
VocabTest/
├── english-vocabulary/        # 已 clone 的原词库（不打包进前端）
├── scripts/
│   └── build-word-data.js     # 预处理词库，生成 src/data/levels/*.json
├── src/
│   ├── data/levels/           # 预处理后的 10 档词汇 JSON
│   ├── pages/
│   │   ├── Home.tsx           # 首页
│   │   ├── Quiz.tsx           # 测试页
│   │   ├── Result.tsx         # 结果页
│   │   ├── History.tsx        # 历史记录页
│   │   └── Dictation.tsx      # 默写练习页（扩展）
│   ├── components/
│   │   ├── WordCard.tsx       # 单词答题卡片
│   │   ├── LevelChart.tsx     # 分级能力图
│   │   ├── ShareCard.tsx      # 分享卡片模板
│   │   └── ProgressBar.tsx    # 进度条
│   ├── hooks/
│   │   ├── useQuizEngine.ts   # 自适应测试算法核心
│   │   ├── useStorage.ts      # localStorage 封装
│   │   └── useSpeech.ts       # TTS 封装
│   ├── utils/
│   │   ├── estimator.ts       # 词汇量估算函数
│   │   └── sampler.ts         # 分层抽样 + 干扰项生成
│   ├── App.tsx / main.tsx
│   └── index.css
├── prd.md                     # 本文档
└── package.json / vite.config.ts / tailwind.config.js
```

### 5.4 核心算法（伪代码）
```ts
// 1. 初始化：预载入 L3–L7 词库索引（轻量），其余按需
// 2. 每轮选题：
function pickNext(state: QuizState): Question {
  if (state.answered === 0) return sampleFrom('L4');
  const recent = state.last(5);
  const hitRate = recent.filter(r => r.correct).length / recent.length;
  const curLevel = levelOf(recent[0]);
  if (hitRate >= 0.6) return sampleFrom(nextLevel(curLevel), state.seen);
  if (hitRate <= 0.3) return sampleFrom(prevLevel(curLevel), state.seen);
  return sampleFrom(curLevel, state.seen);
}

// 3. 词汇量估算
function estimate(result: LevelResult[]): number {
  let total = 0;
  for (const r of result) {
    const mastery = r.mastered / r.sampled; // 0..1
    total += mastery * r.levelTotalWords;
  }
  return Math.round(total);
}
```

---

## 6. 实施里程碑

| 阶段 | 交付物 | 预估工作量 |
|------|--------|----------|
| M1 数据准备 | 预处理脚本跑通，`src/data/levels/*.json` 10 档生成完毕，文件总大小校验通过 | 0.5 天 |
| M2 框架搭建 | Vite + React + Tailwind 项目初始化，路由（Home/Quiz/Result/History）骨架，移动端适配完成 | 0.5 天 |
| M3 测试引擎 | `useQuizEngine` + 答题卡片 WordCard + 进度条 + TTS 发音，可完整跑一轮流程 | 1 天 |
| M4 结果页 | 词汇量估算 + 分级能力柱状图 + 学习建议 + 导出 PNG / 单词清单 | 1 天 |
| M5 历史 & 默写 | 历史记录折线图 + 默写纸打印页（左中文右下划线） | 0.5 天 |
| M6 优化 & 部署 | 首屏性能优化（分包懒加载）、深色模式、Service Worker、部署脚本 | 0.5 天 |
| **合计** | | **4 个工作日** |

---

## 7. 验收标准

1. ✅ 首页展示两种测试模式，点击"开始测试"进入 Quiz 页
2. ✅ Quiz 页答题流程顺畅：判断题 → 选择题 → 实时反馈 + 例句；支持键盘操作
3. ✅ 测试结束后正确调用估算公式，词汇量数字与档位采样正确率对应
4. ✅ 结果页显示分级柱状图，每档悬停展示详细抽样数据
5. ✅ 结果页显示 10 行词汇量对照表；根据估算值自动高亮对应行（背景色 + 左侧箭头），高亮行与副标题百分位描述一致
6. ✅ 结果页底部显示微信二维码占位卡（200×200 占位图 + 微信号 + 3 步领取说明 + 「一键保存结果截图」按钮）；占位图与文案可通过 `src/config/wx.ts` 配置，无需改业务代码
7. ✅ 导出 PNG 卡片正确显示数字 + 图表 + 日期水印
8. ✅ 导出未掌握单词 TXT 格式正确：每行「中文释义\t下划线」
9. ✅ 刷新页面后历史记录仍在；清除按钮可清空
10. ✅ Lighthouse 性能分 ≥ 85；首屏 DOMContentLoaded < 1.5s（本地 Network Fast 3G）
11. ✅ 移动端 iPhone SE / 安卓常见分辨率下无元素溢出、无水平滚动条；微信二维码卡独占一屏，显示完整
12. ✅ 关键文字（标题、单词、选项、对照表行内容）完全在 bounding box 内，不被裁切

---

## 8. 风险与缓解

| 风险 | 影响 | 缓解方案 |
|------|------|----------|
| 词库体积过大导致首屏慢 | 用户流失 | 分级懒加载 + 预处理短字段名 + gzip |
| 干扰项出现重复或语义相近 | 题目歧义 | 同档位随机 + 词性匹配；若释义字数差 <2 则重新抽取 |
| 用户乱点导致结果虚高 | 可信度下降 | 加入"判断题+选择题"双关卡；选"认识"必须答对才计数 |
| Web Speech API 在部分浏览器无中英人声 | 体验降级 | 🔊 按钮检测支持度，不支持则隐藏；可接入第三方 TTS 作为备选（V2） |
| localStorage 被清空丢失记录 | 用户困惑 | 提供"导出我的历史（JSON）"手动备份功能 |
